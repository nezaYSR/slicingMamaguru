import React, { useState, useEffect, Fragment, useCallback } from "react";
import { withRouter, Link } from "react-router-dom";

const Offers = () => {

    return (
        <h1>here are the offers</h1>
    )
}

export default withRouter(Offers);
// export default Offers;